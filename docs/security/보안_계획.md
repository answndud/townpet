# 보안 계획

Last updated: 2026-04-04
Owner: Codex (security track)

## Purpose
- Track security hardening work separately from the main delivery plan.
- Keep `PLAN.md` as the global source of truth, and use this file for security-only details.

## Status legend
- `pending`: not started
- `in_progress`: actively being implemented
- `blocked`: waiting on dependency/decision
- `done`: implemented and verified

## Security hardening backlog

| ID | Priority | Task | Risk addressed | Status | DoD | ETA |
|---|---|---|---|---|---|---|
| SEC-001 | P0 | Health endpoint response minimization (`/api/health`) | Internal env/db detail exposure | done | Public response no longer includes `env.missing` or raw db error detail; internal diagnostics requires valid internal token | 0.5-1d |
| SEC-002 | P0 | CSP hardening (`unsafe-inline` removal path) | XSS impact reduction | done | `CSP_ENFORCE_STRICT=1`에서 production enforce CSP가 nonce + `strict-dynamic`로 동작하고, framework inline style 2종만 exact hash allowlist로 예외 허용한 상태에서 hydration/브라우저 smoke 회귀 없이 동작 | TBD |
| SEC-003 | P1 | Password policy uplift + breached password deny | Weak credential risk | done | Register/reset/set use strong password policy (length+complexity+deny-list), login flow keeps backward compatibility | 1-2d |
| SEC-004 | P1 | Login lockout escalation (account + IP dimensions) | Credential stuffing/bruteforce | done | Multi-window throttling keys (`ip`, `account+ip`, `account`) are applied on credentials login with tests | 1-1.5d |
| SEC-005 | P1 | Trusted proxy IP resolution policy | IP spoofing via header trust mismatch | done | `getClientIp` follows explicit trusted chain policy with prod fallback (`TRUSTED_PROXY_HOPS=1`) and test coverage | 0.5-1d |
| SEC-006 | P1 | Guest identity hash hardening (HMAC pepper) | Re-identification/privacy risk | done | Guest hashes support HMAC pepper with legacy SHA-256 fallback matching for existing rows | 1-2d |
| SEC-007 | P1 | Enumeration-safe auth responses + regression tests | Account existence leakage | done | Register API now normalizes duplicate email/nickname signals to same public error code and tests enforce behavior | 1-1.5d |
| SEC-008 | P0 | Dependency security patch uplift (`next`, `@vercel/blob`) | Known upstream vulnerabilities (DoS/resource exhaustion) | done | `pnpm audit --prod`에서 known vulnerability 0건, lint/typecheck/tests 회귀 통과 | 0.5-1d |
| SEC-009 | P1 | Security env preflight automation (`ops:check:security-env`) | Production misconfiguration drift | done | 운영 핵심 보안 변수(CSP/pepper/internal token/upstash/auth secret) 자동 점검 스크립트 추가 및 GUIDE 실행 절차 반영 | 0.5-1d |
| SEC-010 | P0 | User-generated image provenance enforcement | Passive IP/User-Agent leakage via external images | done | 게시글 이미지/업로드 토큰 경로가 1st-party 업로드 URL만 허용하고 외부 이미지 렌더링이 차단됨 | 0.5-1d |
| SEC-011 | P1 | Admin CSV formula neutralization | Spreadsheet formula injection on export | done | auth audit CSV export가 수식 시작 문자를 neutralize하고 회귀 테스트가 존재함 | 0.5d |
| SEC-012 | P1 | Social dev login explicit opt-in | Preview/staging auth bypass | done | dev social login은 `ENABLE_SOCIAL_DEV_LOGIN=1`일 때만 활성화되고 production에서는 validation이 거부함 | 0.5d |
| SEC-013 | P1 | Runtime env enforcement parity | Security preflight/runtime mismatch | done | production startup validation이 pepper/health token/upstash/weak secret을 강제하고, `ops:check:security-env`의 `CSP_RUNTIME_MODE`가 strict enforce/fallback runtime 상태를 정확히 반영하며 배포 문서와 일치함 | 0.5-1d |
| SEC-014 | P0 | Credential change session revocation | Stolen session remains valid after password reset/change | done | `User.sessionVersion` 기반 JWT invalidation이 적용되고 비밀번호 변경/reset 시 version이 증가하며 테스트가 존재함 | 1-2d |
| SEC-015 | P0 | Write-path sanction enforcement parity | Suspended user bypass via direct REST/API write paths | done | 글/댓글/반응/업로드의 인증 write path가 active sanction을 서비스/route 레이어에서 일관되게 거부하고 테스트가 존재함 | 1-2d |
| SEC-016 | P1 | Login abuse observability + step-up defenses | Bruteforce detection/response is too shallow | done | 실패 로그인 이벤트가 운영 감사 경로 또는 동급 로그로 남고 backoff/CAPTCHA/lockout 중 최소 1개가 credentials 로그인에 적용됨 | 1.5-3d |
| SEC-020 | P1 | Registration abuse observability + multi-axis throttling | Signup spam/disposable account bursts | done | 회원가입 경로가 IP/fingerprint/email+IP/email throttling을 적용하고 등록 성공/거절/제한을 auth audit에서 추적 가능함 | 1-2d |
| SEC-021 | P1 | Production deploy security-env preflight gate | Misconfigured production env reaches deploy/build path | done | `build:vercel`가 production Vercel target에서 strict security-env preflight를 먼저 실행하고 failure-path 테스트가 존재함 | 0.5-1d |
| SEC-022 | P2 | Auth audit retention automation | Sensitive auth audit metadata retained indefinitely | done | 인증 감사 로그 cleanup helper/workflow가 추가되고 180일 retention 기준이 운영 문서와 동기화됨 | 0.5-1d |
| SEC-023 | P1 | Moderation visibility parity + comment report coverage | Blocked/sanctioned actors leak through notifications/public profile, comments cannot be reported | done | 알림/공개 프로필/공개 활동이 차단 relation과 severe sanction visibility 정책을 함께 적용하고, `COMMENT` 신고가 관리자 큐/상세/댓글 UI까지 일관되게 지원됨 | 1-2d |
| SEC-024 | P1 | Authenticated write multi-axis throttling + moderation action audit trail | Logged-in multi-account spam bypass and missing moderation traceability | done | 글/댓글/신고 write path가 `user + userIp + sharedIp` throttling을 적용하고, report/sanction/hospital review suspicious events가 `ModerationActionLog`와 admin page에서 추적 가능함 | 1-2d |
| SEC-025 | P1 | Moderation risk tier tightening + hospital review triage queue | Shared-IP spam bursts and hospital review suspicious signals still require manual spelunking | done | authenticated write helper가 `auth-write:user/ip` 전역 burst와 신규계정/최근제재 risk tier를 적용하고, `/admin/hospital-review-flags`에서 병원 후기 의심 신호를 바로 triage 가능함 | 1d |
| SEC-026 | P1 | Authenticated write client fingerprint propagation | Logged-in abuse detection still depends too much on IP-only signals | done | 로그인 글/댓글/신고 write path가 common client fingerprint를 server action/header로 전달하고, throttling이 `auth-write:fingerprint` 및 scope fingerprint key까지 함께 적용함 | 0.5-1d |
| SEC-027 | P1 | Admin surface concealment hardening | `/admin` probe paths expose existence via login redirect or visible access-denied UI | done | 익명 `/admin`/`/api/admin/*`는 middleware에서 `404 + noindex`로 은닉되고, admin pages는 non-moderator 접근 시 `notFound()`로 통일됨 | 0.5d |
| SEC-028 | P1 | Direct moderation automation guardrails | Internal moderation macro can over-issue sanctions or unsafe restores | done | direct moderation API defaults to `AUTOMATED`, auto-sanction is capped at `WARNING/SUSPEND_7D`, restore/post unhide require manual approval, and admin UI explicitly opts into `MANUAL` mode | 0.5-1d |
| SEC-029 | P0 | Safe inline JSON script serialization | Stored XSS via `</script>` breakout in JSON-LD / inline structured data | done | 모든 `application/ld+json`/inline JSON script 주입 지점이 HTML-safe serializer를 사용하고 `</script>`/U+2028/U+2029 회귀 테스트가 존재함 | 0.5-1d |
| SEC-030 | P0 | Comment response minimization + deleted-content redaction | Author email leakage and deleted comment content disclosure | done | 댓글 public/auth payload에서 `author.email`이 제거되고, `DELETED` 댓글은 API 응답 단계에서 placeholder만 노출되며 회귀 테스트가 존재함 | 1-1.5d |
| SEC-031 | P1 | Login callback path strict allowlist | Open redirect after credentials/social sign-in | done | 로그인 callback helper가 앱 내부 single-slash relative path만 허용하고 `//external`/malformed path를 차단하며 회귀 테스트가 존재함 | 0.5d |
| SEC-032 | P1 | Guest-view override hardening | `x-guest-mode` based block/mute filter bypass | done | guest comment read 경로가 relation filter를 유지하거나 안전한 별도 guest contract로 분리되고 blocked/muted author leakage 테스트가 존재함 | 0.5-1d |
| SEC-033 | P1 | Guest public identifier privacy reduction | Network-derived guest identifier disclosure / tracking | done | public feed/detail/comment payload는 부분 IP 기반 identifier를 제거하고 네트워크 비유출 guest alias 또는 일반 익명 표기만 노출함 | 0.5-1d |
| SEC-034 | P1 | Admin-only surface split + audit trail for privileged reads/writes | Moderator overreach and missing traceability on policy/auth audit surfaces | done | policy/auth-audit/ops/personalization/breeds surfaces are admin-only, admin navigation hides privileged links from moderators, and policy updates plus auth-audit view/export actions are recorded in `ModerationActionLog` | 0.5-1d |
| SEC-035 | P1 | Demo auth fallback explicit opt-in | Shared dev/special env auth bypass via ambient demo account env | done | `DEMO_USER_EMAIL` alone no longer enables auth fallback, `ENABLE_DEMO_AUTH_FALLBACK=1`이 함께 있어야 하며, production validation/preflight는 active demo fallback을 거부한다 | 0.5d |
| SEC-036 | P1 | Guest write override hardening | `x-guest-mode` header can force guest write path in non-production | done | guest write routes no longer trust `x-guest-mode`; guest 비밀번호가 있는 수정/삭제만 guest branch를 우선 사용하고, 생성은 현재 세션 기준으로만 처리되며 관련 route tests가 존재함 | 0.5-1d |
| SEC-037 | P1 | Internal diagnostics localhost-only fallback | Missing `HEALTH_INTERNAL_TOKEN` in shared dev can expose detailed health/CSP stats | done | internal diagnostics are accessible only with a valid internal token or on localhost-only non-production requests, and health/CSP stats routes share the same helper/tests | 0.5d |

## Execution order
1. SEC-001
2. SEC-005
3. SEC-002
4. SEC-004
5. SEC-003
6. SEC-006
7. SEC-007
8. SEC-008
9. SEC-009
10. SEC-014
11. SEC-015
12. SEC-016
13. SEC-020
14. SEC-021
15. SEC-022
16. SEC-023
17. SEC-024
18. SEC-025
19. SEC-026
20. SEC-027
21. SEC-028
22. SEC-029
23. SEC-030
24. SEC-031
25. SEC-032
26. SEC-033
27. SEC-034
28. SEC-035
29. SEC-036
30. SEC-037

## Dependency notes
- SEC-002 phased rollout is complete; keep the framework style hash allowlist narrow and re-verify on Next.js upgrades.
- SEC-006 requires a new secret (pepper) and fallback strategy for existing hashed data.
- SEC-007 should be coordinated with product UX copy to avoid confusion.
- SEC-009 strict mode는 `NODE_ENV=production SECURITY_ENV_STRICT=1` 조합으로 운영 기준 실패를 강제한다.
