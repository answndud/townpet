# PROGRESS.md

## 현재 상태

- active: `성능 개선 8단계 루프`.
- 현재 작업: Phase 4 `초기 JS/resource diet`.
- 직전 완료: Phase 3 `홈 첫 화면 데이터 표시 개선`.
- 다음 액션: `/login`, `/`, `/feed/guest`의 route resource count와 JS transfer를 비교하고, 전역 client boundary/불필요한 초기 JS를 줄일 후보를 찾는다.
